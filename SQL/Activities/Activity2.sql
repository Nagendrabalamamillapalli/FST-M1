-- create tables
create table insert_all (
    into salesman values(5005, 'pi varchar2(4000),
    into salesman values(5006, 'mc varchar2(4000),
    into salesman values(5007, 'pa varchar2(4000),
    into salesman values(5003, 'la varchar2(4000)
)
;


-- triggers
-- load data
