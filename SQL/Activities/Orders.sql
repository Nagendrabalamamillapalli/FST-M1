-- create tables
create table create_table_orders (
    order_no int primary key, purc date,
    customer_id int, salesman_id i varchar2(4000)
)
;

create table insert_all (
    into orders values(70001, 150. date,
    into orders values(70009, 270. date,
    into orders values(70002, 65.2 date,
    into orders values(70004, 110. date,
    into orders values(70007, 948. date,
    into orders values(70005, 2400 date,
    into orders values(70008, 5760 date,
    into orders values(70010, 1983 date,
    into orders values(70003, 2480 date,
    into orders values(70012, 250. date,
    into orders values(70011, 75.2 date,
    into orders values(70013, 3045 date
)
;


-- triggers
-- load data
